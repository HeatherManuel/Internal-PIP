# Supabase — Internal PIP project
VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...
# Service role key — server-side scripts only (never expose to browser)
# Find it: Supabase Dashboard → Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...

# Anthropic
VITE_ANTHROPIC_API_KEY=sk-ant-...

# Google Drive (base64-encoded service account JSON — run: base64 -i service-account.json | tr -d '\n')
GOOGLE_SERVICE_ACCOUNT_B64=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50...

# Ads Manager agent
# Anthropic — server-side only (no VITE_ prefix — never exposed to browser)
ANTHROPIC_API_KEY=sk-ant-...
# Windsor.ai — server-side only
WINDSOR_API_KEY=...
# Windsor Facebook account ID (find in Windsor.ai connector settings)
WINDSOR_FACEBOOK_ACCOUNT_ID=2837959129738933
# Cron secret — set any random string; Vercel sends it automatically on cron invocations
CRON_SECRET=your-random-secret-here

# Future integrations
# VITE_META_APP_ID=
# VITE_GOOGLE_CLIENT_ID=
# VITE_ZOOM_CLIENT_ID=
# VITE_GHL_API_KEY=
